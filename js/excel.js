/**
 * Импорт вопросов из файла .xlsx.
 *
 * Раскладка столбцов оставлена как в старой версии, чтобы уже собранные
 * файлы продолжали работать:  A = вопрос | B = ответ | C = картинка
 * вопроса | D = картинка ответа. Первая строка — заголовок, пропускается.
 *
 * TODO(скелет): реализовать
 *   parse(file)              — File -> [{ text, imageUrl, answerText,
 *                              answerImageUrl }]; выкинуть строку заголовка,
 *                              выкинуть пустые строки, ячейку с картинкой
 *                              считать пустой, если это не http-ссылка
 *   importInto(gameId, rows) — заменить вопросы этой игры целиком
 *
 * Отличие от старого кода: половинки с ответом уходят в question_answer,
 * а не в строку вопроса — см. комментарий в sql/001_schema.sql.
 */
Quiz.excel = {};

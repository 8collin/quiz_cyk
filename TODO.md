# Что осталось сделать

Состояние на 28 июля 2026.

**Все этапы 0–5 закрыты и проверены на живых сессиях, а не на глаз.** База
развёрнута, вход работает, состояние грузится и обновляется через realtime,
буззер собран целиком, ведущий может залить вопросы из Excel и провести
раунд от начала до конца.

Заглушек `TODO(скелет)` не осталось. Фундаментные модули (`config`, `dom`,
`db`, `store`, `timing`, `audio`) переделываться не должны.

Что осталось — внизу, в «Открытых вопросах»: вход игроков без аккаунта,
назначение `sound_key`, публикация. Плюс то, что может проверить только
человек: настоящие телефоны, настоящий файл с вопросами и открытие
`index.html` с диска.

Как поднять страницу, войти без пароля и что при этом не работает —
описано в `CLAUDE.md`, раздел «Verifying a change». Прочитайте его до того,
как что-то запускать: там же лежат две ловушки, на которые уже наступили
(`127.0.0.1:8000` занят Unreal, консоль двоит сообщения).

Порядок ниже — это и есть предлагаемая последовательность работы: каждый
следующий этап опирается на предыдущий и заканчивается чем-то, что можно
открыть и увидеть своими глазами.

---

## Этап 0. Развернуть Supabase — сделано

- [x] Проект создан, ref `hewggiqyaacyzveocvyo`.
- [x] Схема применена целиком, включая триггер на `auth.users`.
- [x] URL и publishable-ключ вписаны в `js/config.js`.
- [x] Ведущий и два тестовых игрока заведены, роль выдана.
- [x] Политики проверены подменой роли (`set local role authenticated`
      плюс `request.jwt.claims`), а не из-под `postgres` — тот RLS обходит,
      и проверка под ним ничего не значит.

Что всплыло по дороге и уже поправлено:

- **Гранты.** Postgres раздаёт `EXECUTE` роли `PUBLIC` автоматически, а
  PostgREST выставляет наружу всё, до чего дотягивается `anon`. Один
  `grant … to authenticated` ничего не отнимает, поэтому в `003_rls.sql`
  теперь сначала `revoke`, потом раздача поимённо.
- **Первый админ.** `guard_profile_role` запрещал менять `role` всем, кто
  не админ, — а первого админа тогда не назначить никак. Триггер пропускает
  сессии без `auth.uid()`: это не клиент, а SQL Editor или миграция.

Тестовая игра, на которой удобно писать следующие этапы:

```
game_id  75bc0a10-80b0-4c9e-b70a-ea284d4499ef   'Тестовая', status=running
         два вопроса с ответами, два участника, счёт 0
ведущий  host@quiz.local
игроки   player1@quiz.local, player2@quiz.local
```

Отдельно стоит помнить: **`UPDATE`, зарубленный политикой, ошибки не даёт** —
он молча меняет ноль строк. Проверять надо результат, а не наличие
исключения. Ошибку `42501` возвращает только `INSERT`.

---

## Этап 1. Вход — написан, в браузере не проверен

Файлы: `js/auth.js`, `js/ui/login.js`, `js/main.js`, плюс три запроса в
`js/db.js`.

- [x] `Quiz.auth.signIn(email, password)` через
      `supabase.auth.signInWithPassword`, затем загрузка строки `profile`
      в `Quiz.store`.
- [x] `Quiz.auth.restoreSession()` при старте — SDK хранит сессию сам,
      после перезагрузки пароль спрашивать заново не нужно.
- [x] `Quiz.auth.signOut()` + `Quiz.store.reset()`.
- [x] Экран входа: обработчик отправки формы, различить неверный пароль и
      сетевой сбой, спрятать `#screen-login` при появлении сессии.
- [x] `main.js`: `db.init()`, `audio.load()`, привязка ползунка громкости,
      выставление класса роли на `<body>`.
- [x] Игрок после входа вызывает `join_game(game_id)` — строка
      `participant` создаётся ему самому. Функция идемпотентна, так что
      звать её при каждом старте можно и нужно.
- [ ] **Открыть страницу и проверить глазами.** Ниже список того, что
      должно случиться.

Причины отказа при входе разведены кодами (`Quiz.auth.FAIL_*`), а тексты
для человека лежат в `Quiz.ui.login.MESSAGES`. Сообщения Supabase
английские и говорят о внутренностях — показывать их нельзя.

Активная игра ищется как самая свежая со `status = 'running'`
(`db.getActiveGame`). Это заглушка до этапа 4, где появится выбор игры;
настройка с id игры была бы таким же временным решением, только её
пришлось бы ещё и вычищать из конфига.

**Готово, когда:** захожу ведущим — вижу десктопную раскладку с пустой
игрой; захожу игроком — телефонную; перезагрузка не разлогинивает;
неверный пароль и отсутствие сети дают разные сообщения; после входа
игроком в `participant` появляется строка, а повторный вход её не
дублирует.

---

## Этап 2. Загрузка состояния и realtime — написан, не проверен

Файлы: `js/db.js`, `js/realtime.js`, `js/ui/question.js`, `js/ui/players.js`.

- [x] Запросы в `db.js`: вопросы, участники, buzz, журнал, открытый ответ.
- [x] Первичная загрузка: `timing.syncClock()` раньше всего остального.
- [x] `realtime.subscribe(gameId)` — подписки на `game`, `participant`,
      `buzz`, `answer_log` плюс канал broadcast для интро.
- [x] Отрисовка вопроса и строки состояния, включая пустое состояние.
- [x] Отрисовка списка игроков через `createElement`. Игроку сортировка по
      очкам, ведущему по имени.
- [x] **Проверено на двух живых сессиях** (ведущий и игрок, разные origin).
      Смена вопроса, показ ответа, счёт и захват буззера доезжают на оба
      экрана без перезагрузки; ось замерзает на обеих сторонах; в консоли
      чисто, канал ровно один.

Первичная загрузка и обработчик смены вопроса зовут одну и ту же
`realtime.reloadQuestionScoped()`. Держать два отдельных пути — верный
способ получить расхождение, которое проявится один раз в середине игры.

`ui/question.js` не смотрит ни на роль, ни на `show_answer`: панель
показывается, когда в store лежит ответ, а попал он туда или нет — уже
решила политика `question_answer_select_when_revealed`.

**Готово, когда:** меняю строку в таблице — оба экрана обновляются сами,
без перезагрузки.

**Панель ответа — решено.** Класс `admin-only` с неё снят. Это не «показали
игроку ответ», а перестали дублировать правило: кому и когда видна строка
`question_answer`, уже решает политика — ведущему всегда, игроку только
после «Ответ». Разметка знала то же самое вторым, противоречащим способом.

Проверено на живой сессии игрока: при `show_answer = false` панели нет,
после нажатия она появляется с текстом ответа. Отступы и кегль на телефоне
переопределены в `player.css` — размеры ведущего заняли бы полэкрана.

---

## Этап 3. Буззер — сделан и проверен

Файлы: `js/buzzer.js`, `js/ui/buzzer.js`.

- [x] `Quiz.buzzer.hit()`: локальные предохранители, мгновенная блокировка
      кнопки против двойного тапа, INSERT, разбор ошибки по коду
      (`23505` — опередили, `P0001` — кулдаун, синхронизировать часы).
- [x] Четыре состояния кнопки и тикающий обратный отсчёт.
- [x] Интервал гасится, когда кулдаун кончился или игрок вышел.

Проверено на трёх живых сессиях (ведущий и два игрока, три разных origin):

| Что | Результат |
|---|---|
| Три `hit()` подряд в одном такте | ушёл **один** запрос, кнопка погасла синхронно |
| INSERT при занятом слоте | `23505`, кнопка остаётся «ИДЁТ ОТВЕТ...» |
| INSERT при разъехавшемся кеше | `P0001` → пересинхрон часов, штраф восстановлен |
| Отсчёт 3 с | 3.0 → 2.2 → 1.2 → 0.4 → «ЕСТЬ ОТВЕТ!», таймер убит сам |
| 15 тиков отсчёта | **0** сетевых вызовов |

**Залипание «ОТВЕЧАЙТЕ», найдено и починено после этой таблицы.** Ведущий
оценивал ответ быстрее трёх секунд с нажатия — и кнопка игрока оставалась
зелёной «ОТВЕЧАЙТЕ» насовсем, до перезагрузки страницы. Причина не в
realtime: все четыре события доезжали. В `Quiz.buzzer.state()` флаг
`pending` проверялся раньше `Quiz.store.buzz` и выходил из функции
досрочно, поэтому приезд строки его не снимал — снимал только таймаут в
3 с. А таймаут считается там же, то есть на перерисовке, а состояние MINE
не перерисовывается ничем: интервал идёт только в WAIT. Последнее событие
оценки приходило внутри окна — и будить кнопку становилось некому.

Починено двумя правками в `js/buzzer.js`: строка из базы проверяется
первой (авторитет у неё, а `pending` — предохранитель ровно на промежуток
«тап ушёл, строка не вернулась»), и `hit()` сам заводит одноразовую
перерисовку на истечение флага, иначе таймаут существует только на бумаге.

Проверено на трёх живых сессиях, оценка приходила через 246 мс после
нажатия — вдвое глубже прежнего окна:

| Что | До правки | После |
|---|---|---|
| Оценка через 0.2 с после нажатия | «ОТВЕЧАЙТЕ» навсегда, штраф истёк, кнопка мертва | «ПОДОЖДИТЕ 4.2 → 3.2 → 1.8 → 0.4» → «ЕСТЬ ОТВЕТ!» |
| Проигравший гонку (оба нажали в одну метку) | «ОТВЕЧАЙТЕ» до 3 с, с тем же риском залипнуть | «ИДЁТ ОТВЕТ...» |

Заодно видно то, что правкой не затронуто: проигравший на ~300 мс
показывает «ЕСТЬ ОТВЕТ!», если ответ `23505` обгоняет чужую строку из
realtime. Кнопка при этом честна — своей строки нет, чужая не приехала, —
а повторный тап упрётся в тот же `23505` без последствий.

Гонку вживую поймать не удалось, и это хорошая новость: realtime приносит
чужую строку быстрее, чем уходит второй запрос, так что проигравший
упирается в локальный предохранитель и в базу не стучится вовсе. Ветку
`23505` пришлось проверять принудительным INSERT в занятый слот — ошибка
при этом настоящая, серверная, и через настоящий обработчик.

**Осталось от человека:** нажать с двух настоящих телефонов. Три вкладки
на одной машине ловят логику, но не ловят реальные задержки сети.

---

## Этап 4. Панель ведущего — сделана и проверена

Файлы: `js/game.js`, `js/ui/admin.js` (новый), `js/ui/players.js`, плюс
записи ведущего в `js/db.js`.

- [x] Навигация по вопросам, после каждой смены — `reset_thinking()`.
- [x] Показ и скрытие ответа (`show_answer`).
- [x] Оценка: запись в `answer_log` и освобождение слота. Снятие
      отвечающего без оценки попыткой не считается.
- [x] Отмена случайного нажатия (`reduce_penalty`) и сброс всех ожиданий.
- [x] Добавление игрока без аккаунта, удаление участника.
- [x] Создание игры и выбор активной из списка своих.
- [x] Сброс игры.
- [x] Интро: локально + broadcast всем.

**Очки начисляет база, и до этого этапа их не начислял никто.** Триггер
`answer_applies_penalty` ставил только штраф — оценка ведущего никуда не
доезжала. Теперь это `answer_applies_effects`: тем же UPDATE и счёт, и
штраф. На клиент это выносить нельзя — PostgREST не умеет
`score = score + delta`, пришлось бы слать сумму от закешированного
значения и терять чужую правку.

**Порядок запросов в двух местах не косметический.**

- *Смена вопроса:* сначала `reset_thinking()`, потом указатель. Наоборот —
  и перечитка успевает прочитать буззер от прошлого вопроса, а подсветка
  «отвечает» залипает. Это те самые грабли старой версии.
- *Оценка:* сначала журнал, потом освобождение слота. Пока слот занят, ось
  заморожена, и штраф считается от того же T, на котором игрок нажал.
- *Смена активной игры:* новая получает `running` раньше, чем старая его
  теряет. Иначе между двумя запросами нет ни одной идущей игры — и игрок,
  среагировавший на снятие статуса, останется на «игра не найдена» насовсем.

Список вопросов теперь перечитывается вместе со сменой указателя
(`reloadQuestionScoped`): таблица `question` в публикацию realtime не
входит, и без этого залитые вопросы не доехали бы до телефонов вовсе. На
этапе 5 импорт обязан обновить и свой `store.questions` — до перечитки
ведущий видит пустой список и не может никуда перейти.

Проверено на трёх живых сессиях (ведущий и два игрока, три разных origin):

| Что | Результат |
|---|---|
| «▶» | вопрос сменился на обоих телефонах, ось сброшена в 0, ответ скрылся |
| «Ответ» / «Скрыть» | панель у игрока появилась и исчезла, текст вычищен из DOM |
| «+1» отвечающему | счёт 0 → 1 везде, слот свободен, штраф от **замороженной** оси |
| «Снять» | слот свободен, в журнале пусто, кулдаун не тронут |
| «⏱−» | ровно один шаг штрафа назад |
| «Сброс ⏱» | ожидания в ноль, ось в ноль |
| Бейдж ожидания, 15 тиков | **0** сетевых вызовов, 3.1 → 0.1 |
| Бейдж, пока кто-то отвечает | 26.2 → 26.2 за 2.5 с — ось стоит |
| Имя `Гость "Кавычка"` | доехало как текст, разметку не сломало |
| «+ Игра» | обе вкладки игроков переехали сами, по одному каналу |
| «Сброс игры» | вопросы, ответы и журнал стёрты, счёт в ноль, экраны следом |
| Интро | ровно по одному вызову `play('intro')` в каждой из трёх вкладок |

**Осталось от человека:** провести раунд с настоящих телефонов.

**Решено по ходу.** «Сброс игры» стирает и вопросы — это возвращение игры
к чистому листу, дальше нужен новый импорт. Кнопка спрашивает
подтверждение, потому что вместе с вопросами каскадом уходят ответы и
журнал.

---

## Этап 5. Импорт вопросов из Excel — сделан и проверен

Файлы: `js/excel.js` (разбор), `db.replaceQuestions` (запись),
`game.importQuestions` (порядок действий), `ui/admin.js` (файл и тексты).

- [x] Разбор `.xlsx`: столбцы A = вопрос, B = ответ, C = картинка вопроса,
      D = картинка ответа; первая непустая строка — заголовок.
- [x] Заливка одним набором, ответы уходят в `question_answer`.
- [x] Понятная ошибка, если файл пустой или в неверном формате.

Разбор идёт **до** первой записи: из битого файла в базу не должно попасть
ничего, в том числе половины набора. Проверено — после трёх неудачных
попыток подряд прежние вопросы на месте.

**О формате судим по имени файла, а не по содержимому.** `XLSX.read`
почти никогда не падает: текстовый файл он молча перечитывает как CSV и
отдаёт лист «Sheet1» с одной строкой мусора. Без проверки расширения
человек, выбравший заметку вместо таблицы, получал «в файле нет строк с
вопросами» и шёл искать вопросы не в том файле.

**Строка из одних пробелов считается пустой** — здесь мы намеренно
расходимся со старой версией, которая сравнивала с `''` и пропускала такую
строку дальше, а ведущий получал посреди игры вопрос без текста.

**Вскрылась гонка, которой не видно было на этапе 4.** `subscribe()`
возвращался до того, как канал реально присоединился, поэтому запись,
сделанная сразу после переключения игры, проходила мимо подписки: в базе
всё верно, а на экране прошлое состояние. Ловится это только на цепочке
«перечитал игру и тут же что-то сделал», то есть на импорте. Теперь
`subscribe()` отдаёт промис, а `loadGame` его ждёт.

Проверено на трёх живых сессиях, файлы собирались прямо в странице той же
библиотекой, что их читает, и скармливались настоящему `<input type=file>`:

| Что | Результат |
|---|---|
| Обычный файл, 3 строки | 3 вопроса, ведущий встал на первый, оба игрока следом |
| Повторная загрузка | спросила подтверждение, заменила набор целиком |
| 60 вопросов | 60 + 60 ответов, позиции 0…59 без дыр |
| Пустая строка первой | первый вопрос не съеден заголовком |
| Строка из пробелов | выброшена, пустого вопроса в игре нет |
| Пробелы по краям ячеек | обрезаны, ссылка с пробелами уцелела |
| `нет` / `не нашёл` в столбце картинки | стали пустыми, в `src` не попали |
| Только заголовок / пустой файл | «нет строк с вопросами», старые вопросы целы |
| `.txt` вместо таблицы | «не похоже на файл Excel», старые вопросы целы |
| Импорт из-под игрока | `42501` от политики; DELETE до этого тоже не прошёл |

**Осталось от человека:** залить настоящий файл с вопросами. Проверял я на
файлах, собранных в браузере, — на машине ни одного `.xlsx` не нашлось.
Раскладка столбцов и правила разбора взяты из `cyk.html` строка в строку,
но подтвердить «старый файл заливается без правок» может только сам файл.

---

## Решённые вопросы

**Как игрок попадает в игру.** Игрок сам жмёт «войти» — вызывается
`public.join_game(game_id)`. Писать в `participant` напрямую ему нельзя
(иначе он правил бы себе счёт), поэтому строку заводит `security definer`
функция, подставляя `profile_id` из `auth.uid()`. Ведущий по-прежнему
добавляет «ручных» игроков без аккаунта. Задача — в этапе 1.

**Как создаётся сама игра.** Кнопка «+ Игра» у ведущего: обычный INSERT в
`game` с `host_id = auth.uid()`, отдельной функции не понадобилось.
Сделано в этапе 4.

**Идущая игра ровно одна.** По ней игроки находят, куда заходить
(`getActiveGame` берёт самую свежую со `status = 'running'`), поэтому
активация игры снимает статус со всех остальных игр ведущего. Выбор
активной — выпадающий список в шапке.

---

## Открытые вопросы — решить до соответствующего этапа

**Заводит ли игрок аккаунт сам.** Отложено на этапе 4 сознательно: пока
аккаунты создаёт ведущий через дашборд, регистрации в интерфейсе игры нет.
Схема остаётся half-self-service — присоединиться к игре игрок может сам
(`join_game`), а появиться на свет нет. Варианты, когда до этого дойдут
руки: `signUp` с почтой и паролем (`display_name` уходит в `options.data`,
триггер его подхватит) или `signInAnonymously()` — игрок вводит только имя,
пароля нет вовсе, а аккаунт настоящий, так что `join_game` и все политики
работают без правок. Второе ближе к аудитории «люди в комнате».

**Как назначается `sound_key`.** Интерфейса нет. Проще всего — поле в
карточке участника у ведущего.

**Имена звуковых файлов.** Перенесены как есть, вместе с `Алина old.mp3`,
`Чебурек old.mp3` и `intro2.mp3`, на которые в старом коде не было ни
одной ссылки. Раз теперь есть `sound_key`, файлы можно переименовать во
что-то нейтральное — удобно сделать заодно с предыдущим пунктом.

**Публикация.** Репозитория на GitHub пока нет, remote не настроен,
GitHub Pages не подключён. Отложено сознательно.

---

## Чего делать не нужно

- Не добавлять сборку, TypeScript, npm и ES-модули — см. `CLAUDE.md`.
- Не переносить логику из RLS-политик в клиентский код.
- Не решать гонку за буззером на клиенте.
- Не заводить отдельную страницу для игрока: роль — это класс на `<body>`.

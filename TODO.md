# Что осталось сделать

Состояние на 28 июля 2026: собран скелет. Схема, вёрстка и фундаментные
модули (`config`, `dom`, `db`, `store`, `timing`, `audio`) написаны и
переделываться не должны. Игровая логика заглушена — все заглушки помечены
`TODO(скелет)` и содержат описание того, что от них ожидается.

Порядок ниже — это и есть предлагаемая последовательность работы: каждый
следующий этап опирается на предыдущий и заканчивается чем-то, что можно
открыть и увидеть своими глазами.

---

## Этап 0. Развернуть Supabase — сделать до всего остального

Без этого ни один следующий пункт не проверить.

- [ ] Создать проект в Supabase.
- [ ] Выполнить `sql/001_schema.sql`, `sql/002_functions.sql`,
      `sql/003_rls.sql` в SQL Editor **в этом порядке**.
- [ ] Вписать URL и anon-ключ в `js/config.js`.
- [ ] Завести аккаунт ведущего и выдать роль:
      `update public.profile set role='admin' where id='<uuid>';`
- [ ] Завести пару тестовых аккаунтов игроков.

**Готово, когда:** запросы из консоли браузера возвращают данные, а
попытка игрока изменить чужой счёт отваливается по RLS.

Схема ни разу не прогонялась на живой базе — на первом запуске возможны
опечатки и проблемы с порядком объектов. Это ожидаемо, правим по месту.

---

## Этап 1. Вход

Файлы: `js/auth.js`, `js/ui/login.js`, `js/main.js`.

- [ ] `Quiz.auth.signIn(email, password)` через
      `supabase.auth.signInWithPassword`, затем загрузка строки `profile`
      в `Quiz.store`.
- [ ] `Quiz.auth.restoreSession()` при старте — SDK хранит сессию сам,
      после перезагрузки пароль спрашивать заново не нужно.
- [ ] `Quiz.auth.signOut()` + `Quiz.store.reset()`.
- [ ] Экран входа: обработчик отправки формы, различить неверный пароль и
      сетевой сбой, спрятать `#screen-login` при появлении сессии.
- [ ] `main.js`: `db.init()`, `audio.load()`, привязка ползунка громкости,
      выставление класса роли на `<body>`.

**Готово, когда:** захожу ведущим — вижу десктопную раскладку с пустой
игрой; захожу игроком — телефонную; перезагрузка не разлогинивает.

---

## Этап 2. Загрузка состояния и realtime

Файлы: `js/db.js`, `js/realtime.js`, `js/ui/question.js`, `js/ui/players.js`.

- [ ] Дописать запросы в `db.js` — список запланированных лежит
      комментарием в конце файла.
- [ ] Первичная загрузка: `timing.syncClock()` **раньше всего остального**,
      затем игра, вопросы, участники, buzz, журнал ответов.
- [ ] `realtime.subscribe(gameId)` — подписки на `game`, `participant`,
      `buzz`, `answer_log` плюс канал broadcast для интро.
- [ ] Отрисовка вопроса и строки состояния, включая пустое состояние.
- [ ] Отрисовка списка игроков. Строить через `createElement`, не склейкой
      HTML. Игроку сортировка по очкам, ведущему по имени.

**Готово, когда:** меняю строку в таблице через Supabase Studio — оба
экрана обновляются сами, без перезагрузки.

---

## Этап 3. Буззер

Файлы: `js/buzzer.js`, `js/ui/buzzer.js`.

- [ ] `Quiz.buzzer.hit()`: локальные предохранители, мгновенная блокировка
      кнопки против двойного тапа, INSERT, разбор ошибки по коду
      (`23505` — опередили, `P0001` — кулдаун, синхронизировать часы).
- [ ] Четыре состояния кнопки и тикающий обратный отсчёт.
- [ ] Интервал гасится, когда кулдаун кончился или игрок вышел.

**Готово, когда:** двое жмут одновременно с двух телефонов — ровно один
становится отвечающим, второй видит «ИДЁТ ОТВЕТ...». Проверить именно на
двух устройствах, две вкладки на одной машине этот случай не ловят.

---

## Этап 4. Панель ведущего

Файл: `js/game.js`, плюс кнопки в `js/ui/players.js`.

- [ ] Навигация по вопросам, после каждой смены — `reset_thinking()`.
- [ ] Показ и скрытие ответа (`show_answer`).
- [ ] Оценка: запись в `answer_log` (штраф наложит триггер) и освобождение
      слота. Снятие отвечающего без оценки попыткой не считается.
- [ ] Отмена случайного нажатия (`reduce_penalty`) и сброс всех ожиданий.
- [ ] Добавление игрока без аккаунта, удаление участника.
- [ ] Перезапуск игры.
- [ ] Интро: локально + broadcast всем.

**Внимание на грабли из старой версии:** после смены вопроса realtime по
`buzz` и `participant` может отстать от строки `game`, и подсветка
«отвечает» с бейджами ожидания залипает. Перечитывать эти таблицы явно.

**Готово, когда:** можно провести раунд от начала до конца.

---

## Этап 5. Импорт вопросов из Excel

Файл: `js/excel.js`.

- [ ] Разбор `.xlsx`: столбцы A = вопрос, B = ответ, C = картинка вопроса,
      D = картинка ответа; первая строка — заголовок.
- [ ] Заливка в базу одной операцией, ответы уходят в `question_answer`.
- [ ] Понятная ошибка, если файл пустой или в неверном формате.

**Готово, когда:** старый рабочий файл с вопросами заливается без правок.

---

## Открытые вопросы — решить до соответствующего этапа

**Как игрок попадает в игру.** Сейчас `handle_new_user` создаёт `profile`,
но строка `participant` не появляется ниоткуда. Варианты: ведущий
добавляет из списка зарегистрированных; игрок сам жмёт «войти в игру» и
строка создаётся ему; код игры. Влияет на этап 1 и на политику
`buzz_insert_self`.

**Как создаётся сама игра.** Экрана создания нет, а `game.host_id`
обязателен. На первое время можно завести строку руками через Studio, но
для этапа 4 нужен ответ.

**Как назначается `sound_key`.** Интерфейса нет. Проще всего — поле в
карточке участника у ведущего.

**Имена звуковых файлов.** Перенесены как есть, вместе с `Алина old.mp3`,
`Чебурек old.mp3` и `intro2.mp3`, на которые в старом коде не было ни
одной ссылки. Раз теперь есть `sound_key`, файлы можно переименовать во
что-то нейтральное — удобно сделать заодно с предыдущим пунктом.

**Публикация.** Репозитория на GitHub пока нет, remote не настроен,
GitHub Pages не подключён. Отложено сознательно.

---

## Чего делать не нужно

- Не добавлять сборку, TypeScript, npm и ES-модули — см. `CLAUDE.md`.
- Не переносить логику из RLS-политик в клиентский код.
- Не решать гонку за буззером на клиенте.
- Не заводить отдельную страницу для игрока: роль — это класс на `<body>`.
